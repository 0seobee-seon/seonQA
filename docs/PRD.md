# PRD — 선엔지니어링 총무팀 업무 안내 챗봇 MVP v1

**작성일:** 2026-06-18  
**최종 업데이트:** 2026-06-19  
**작성자:** 총무팀 김영섭 차장  
**상태:** MVP 구현 완료 (T-01 ~ T-06 완료 / T-07 선택 대기)

---

## 1. Problem Statement (문제 정의)

선엔지니어링 총무팀으로 직원들의 반복 문의가 집중되어 핵심 업무가 지체되고 있다.  
대부분의 문의는 사내 규정집·양식 문서로 답변 가능하지만, 직원들이 문서를 직접 찾지 않고  
담당자에게 질문하는 패턴이 반복된다.

> **핵심 흐름 1개:** 직원이 질문을 입력 → 사내 문서에서 관련 내용 검색 → 답변 출력

---

## 2. Target User (목표 사용자)

**첫 사용자:** 선엔지니어링 전 직원 (현장 포함)

**실제로 반복되는 업무 문의 유형:**

| 분류 | 대표 문의 |
|------|---------|
| 인사/복무 | 연차 신청, 반차, 출장, 복무규정 |
| IT | 그룹웨어 비밀번호 재설정, 전산기기 신청 |
| 총무 | 사내 전화번호, 문서번호 발급, 렌트차량, 사무가구 |
| 규정 | 급여규정, 인사관리규정, 취업규칙 |
| 양식 | 사직원, 차용증, 연봉변경계약서 |

---

## 3. User Flow (사용자 흐름)

```
1. 직원이 http://[사내주소] 접속
        ↓
2. 질문 입력 또는 상단 빠른 버튼 클릭
   예: "연차 신청 어떻게 해요?"
        ↓
3. 서버가 database/ 폴더의 사내 문서에서 관련 내용 검색
        ↓
4-A. 관련 내용 있음 → 질문 키워드 주변 발췌 답변 + 출처 문서명 표시
4-B. 관련 내용 없음 → "총무팀 문의" 안내 (이메일·내선 표시)
        ↓
5. 직원이 답변 확인 후 총무팀 연락 없이 스스로 처리
```

---

## 4. Core Features (핵심 기능)

### F-1. 질문 입력 및 답변 출력 ✅
- 텍스트 입력창 + 전송 버튼 (엔터 가능)
- 빠른 질문 버튼 5개: 연차/휴가 신청, 그룹웨어 비밀번호 재설정, 사내 전화번호 확인, 복무규정 확인, 취업규칙 확인
- 사용자 말풍선(우측) / 봇 말풍선(좌측) 구분
- 로딩 애니메이션(점 3개)

### F-2. 사내 문서 기반 답변 ✅
- `database/` 폴더의 실제 선엔지니어링 문서에서 내용 추출
- 지원 형식: PDF (`pdf-parse@1.1.1`), hwpx (`adm-zip` + XML `<hp:t>` 태그 파싱)
- **스마트 발췌:** 질문 키워드가 등장하는 주변 텍스트(±40~80자)만 반환 — 문서 전체 반환 방지
  - 문서 파일명에서 뽑은 키워드(문서 선택용)는 발췌 검색에서 제외
  - 예: "홍진욱 전화번호" 질문 시 "전화번호"는 파일명 키워드라 제외, "홍진욱" 주변만 발췌
- 답변 하단에 출처 문서명 표시
- API 키 없을 때: 문서 발췌 직접 출력 (목업 모드)
- API 키 있을 때: Claude API (`claude-sonnet-4-6`)로 자연어 요약 답변

### F-3. Fallback 안내 ✅
- 문서에서 내용을 찾지 못한 경우 자동으로 총무팀 연락처 안내
- 📧 kimm1027@seon.co.kr / 📞 043-220-8505
- "사람 확인 필요" 판단은 자동 결론으로 표시하지 않음

---

## 5. Required Data (필요한 데이터)

### 현재 가진 데이터 (`database/` 폴더) — 15개 파일 전체 로드 완료

| 파일명 | 분류 | 형식 | 추출 글자 수 |
|--------|------|------|------------|
| 그룹웨어 비밀번호 재설정 | IT | PDF | 938자 |
| 선엔지니어링 사내 전화번호 | 총무 | hwpx | 2,547자 |
| 입사자 교육 자료(인터넷 가입)(수정) | IT | hwpx | 1,733자 |
| 급여규정18년 | 규정 | hwpx | 4,129자 |
| 복무규정20년 | 규정 | hwpx | 9,914자 |
| 인사고과 규정18년 | 규정 | hwpx | 5,293자 |
| 인사관리규정18년 | 규정 | hwpx | 12,176자 |
| 취업규칙(2026)최종본 | 규정 | hwpx | 20,480자 |
| 양식 7개 (사직원, 차용증 등) | 양식 | hwpx | 각 수백~수천자 |

### 사용자가 입력할 데이터
- 자연어 질문 텍스트 (최대 200자)

### 외부에서 가져와야 할 데이터/API
- Anthropic Claude API — 자연어 요약 답변 생성
- **현재 상태:** 코드 연동 완료, API 키 미발급 (T-07 대기)

### 샘플 데이터 (현재 미사용)
- `data/guide.json` — 9개 업무 유형 샘플, 실제 문서로 교체 완료

---

## 6. Definition of Done (완료 기준)

### 완료 체크리스트

- [x] `database/` 폴더의 PDF 파일을 읽어 답변에 활용한다
- [x] `database/` 폴더의 hwpx 파일을 읽어 답변에 활용한다
- [x] 답변 하단에 실제 선엔지니어링 문서명이 출처로 표시된다
- [x] 문서에 없는 질문은 fallback 메시지로 처리된다
- [x] 브라우저에서 http://localhost:3001 접속 시 챗봇 화면이 열린다
- [x] 목업 모드(API 키 없음)에서도 문서 내용 기반 답변이 나온다
- [x] 특정 항목(이름 등) 질문 시 해당 부분만 발췌해서 반환한다

### 직접 확인한 테스트 케이스

| # | 입력 | 결과 | 상태 |
|---|------|------|------|
| T-1 | "그룹웨어 비밀번호 재설정 방법" | 실제 PDF 내용 기반 답변 + 출처 표시 | ✅ |
| T-2 | "복무규정에서 연차는 며칠인가요" | 복무규정20년.hwpx 내용 기반 답변 + 출처 표시 | ✅ |
| T-3 | "오늘 점심 뭐 먹을까요" | 노란 fallback 박스 + 총무팀 이메일·내선 안내 | ✅ |
| T-4 | "홍진욱 전화번호" | 해당 이름 주변 발췌 (526번) | ✅ |

---

## 7. 기술 구현 현황

### 파일 구조

```
챗봇 만들기/
├── index.html              # 단일 페이지 채팅 UI
├── style.css               # 채팅 말풍선 스타일 (헤더 #1A56DB)
├── app.js                  # 클라이언트 로직 (fetch, 렌더링)
├── server.js               # Node.js HTTP 서버 (포트 3001)
├── api/
│   └── chat.js             # 키워드 매칭 + 발췌 + 답변 생성
├── utils/
│   └── fileReader.js       # PDF/hwpx 파싱, 문서 캐시
├── database/               # 실제 선엔지니어링 문서 15개
│   ├── 그룹웨어 비밀번호 재설성.pdf
│   ├── 선엔지니어링 사내 전화번호.hwpx
│   ├── 입사자 교육 자료(인터넷 가입)(수정).hwpx
│   └── 규정/, 양식/  (하위 폴더)
├── data/
│   └── guide.json          # 샘플 데이터 (현재 미사용)
├── docs/
│   └── PRD.md              # 이 문서
└── .env                    # ANTHROPIC_API_KEY (미설정)
```

### 주요 기술 결정

| 결정 | 선택 | 이유 |
|------|------|------|
| hwpx 파싱 | adm-zip 직접 파싱 | PDF 변환 없이 즉시 처리 가능 |
| PDF 파싱 | pdf-parse@1.1.1 | v2 API 변경으로 다운그레이드 |
| 키워드 매칭 | 파일명 기반 키워드 추출 | 별도 태깅 없이 자동화 |
| 발췌 방식 | 질문 단어 주변 ±80자 | 문서 전체 반환 방지 |
| 서버 | Node.js 내장 http | Express 없이 경량 구현 |

---

## 8. Nope (이번에 안 만들 기능)

| 기능 | 하지 않는 이유 |
|------|-------------|
| 로그인 / 사용자 인증 | MVP 범위 초과, 사내 인트라넷 접근 제한으로 대체 |
| 관리자 파일 업로드 UI | 담당자가 폴더에 직접 파일 추가하는 방식으로 대체 |
| 대화 이력 저장 (DB) | 현재 단계에서 불필요, 세션 내 대화로 충분 |
| 모바일 반응형 UI | PC 웹 우선 검증 후 2단계에서 추가 |
| 결제 기능 | 해당 없음 |
| 자동 판단·자동 승인 | 업무 판단은 사람이 최종 확인하는 원칙 유지 |
| 실시간 문서 동기화 | 파일 추가 시 서버 재시작으로 갱신 (MVP 허용 범위) |

---

## 9. 막힌 점 및 해결 기록

| # | 이슈 | 해결 방법 | 상태 |
|---|------|---------|------|
| I-1 | pdf-parse v2 API 변경으로 `require()` 오류 | `pdf-parse@1.1.1`로 다운그레이드 | ✅ 해결 |
| I-2 | hwpx 파일 텍스트 추출 방법 미결정 | adm-zip으로 ZIP 열기 → section XML → `<hp:t>` 태그 추출 | ✅ 해결 |
| I-3 | 키워드 매칭 오류 ("복무규정에서…" → 미매칭) | 파일명 숫자 기준 분리 추가 (`복무규정20년` → `복무규정` 추출) | ✅ 해결 |
| I-4 | 전화번호 조회 시 문서 전체 반환 | 질문 키워드 주변 발췌 함수 추가, 문서 선택 키워드는 제외 | ✅ 해결 |
| I-5 | 이름 검색 시 문서 미매칭 ("김영섭 차장 내선번호" → fallback) | ① hwpx 한글 글자 사이 공백 제거 (`"김 영 섭"` → `"김영섭"`) ② 파일명 키워드 미매칭 시 본문 전체 검색으로 2단계 폴백 추가 | ✅ 해결 |
| I-6 | 배포 위치 미확인 (인트라넷 vs 홈페이지) | IT팀 확인 필요 | ⬜ 미확인 |
| I-7 | Claude API 키 미발급 | T-07에서 진행 (선택) | ⬜ 미착수 |

---

## 10. 버전 현황

| 구분 | MVP (v1) | seonQA (v2) |
|------|----------|-------------|
| 저장소 | 로컬 폴더 | [github.com/0seobee-seon/seonQA](https://github.com/0seobee-seon/seonQA) |
| 배포 | localhost:3001 | [seon-qa.vercel.app](https://seon-qa.vercel.app) (GitHub push → 자동 배포) |
| 기술 스택 | Node.js, Vanilla JS | Next.js 16, React 19, TypeScript, Tailwind CSS |
| 데이터 | database/ 실제 문서 (PDF, hwpx) 파싱 | faq.ts 정적 데이터 (9개 FAQ) |
| 백엔드 | Node.js HTTP 서버 | 없음 (프론트엔드 전용) |
| AI 연동 | Claude API (선택, .env) | 미연동 |
| DB | 없음 | Supabase (환경변수 준비, 코드 미연동) |

### seonQA 로컬 실행 방법

```bash
# 1. 클론 (최초 1회)
git clone https://github.com/0seobee-seon/seonQA.git
cd seonQA
npm install

# 2. 환경변수 파일 생성 (.env.local)
NEXT_PUBLIC_SUPABASE_URL=https://gfkblaewaosemeolvllr.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_wWxhe-FNgnaamiojb66omw_bbjtFE7V

# 3. 실행
npm run dev  →  http://localhost:3000
```

### seonQA 코드 수정 후 배포

```bash
git add .
git commit -m "수정 내용"
git push  →  Vercel 자동 재배포
```

### seonQA 데이터 구조 (`src/app/data/faq.ts`)

카테고리 4개 (인사 / 총무 / 구매 / 회계), FAQ 9개 항목  
각 항목: `question`, `keywords`, `answer`, `contact` 필드

---

## 11. 파일 백업 위치

| 위치 | 경로 | 비고 |
|------|------|------|
| 작업 원본 | `C:\Users\LG\Desktop\AX 코드 학습\챗봇 만들기` | 개발 작업 디렉터리 |
| 노트북 백업 | `D:\노트북 작업\챗봇 만들기` | 로컬 D드라이브 |
| 클라우드 백업 | `G:\내 드라이브\AI 영섭 작업기록\질문 챗봇\챗봇 만들기` | 구글 드라이브 |

> `node_modules` 폴더는 백업에서 제외. 다른 PC에서 실행 시 `npm install` 필요.

---

## 12. 다음 단계 (Next Steps)

| 우선순위 | 작업 | 조건 |
|---------|------|------|
| 선택 | T-07: Claude API 키 입력 후 자연어 답변 확인 | API 키 발급 후 |
| 필수 | 배포 위치 결정 (IT팀 확인) | 서비스 오픈 전 |
| 2단계 | 모바일 반응형 UI | PC 검증 완료 후 |
| 2단계 | 관리자 파일 업로드 UI | 운영 편의성 향상 |
