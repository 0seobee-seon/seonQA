# TODO — 선엔지니어링 총무팀 챗봇 MVP v1

> PRD 기준일: 2026-06-18
> 작업 단위: 30분~1시간
> 진행 상태: ⬜ 미착수 / 🔄 진행 중 / ✅ 완료 / ⛔ 막힘

---

## Phase 1. 화면 확인 (의존성 없음, 지금 바로 가능)

### ✅ T-01. 빠른 버튼을 실제 자료 기준으로 교체 (30분)
**작업:** `index.html`의 빠른 질문 버튼 5개를 database/ 파일 기준으로 바꾼다.

변경 전 → 변경 후:
- 연차/휴가 신청 → 연차/휴가 신청 (유지)
- 비밀번호 재설정 → 그룹웨어 비밀번호 재설정
- 문서번호 발급 → 사내 전화번호 확인
- 전산기기 신청 → 복무규정 확인
- 렌트차량 신청 → 취업규칙 확인

**완료 기준:**
- [ ] 브라우저에서 버튼 5개가 위 내용으로 보인다
- [ ] 버튼 클릭 시 해당 질문이 입력창에 자동 입력된다

---

## Phase 2. PDF 파싱 (T-01 이후 진행)

### ✅ T-02. pdf-parse 라이브러리 설치 및 추출 확인 (30분)
**작업:** PDF 텍스트 추출이 되는지 터미널에서 먼저 확인한다.

```bash
npm install pdf-parse
node -e "
const pdf = require('pdf-parse');
const fs = require('fs');
pdf(fs.readFileSync('./database/그룹웨어 비밀번호 재설성.pdf'))
  .then(d => console.log(d.text.slice(0, 500)));
"
```

**완료 기준:**
- [ ] 터미널에 PDF 내 한글 텍스트가 500자 이상 출력된다
- [ ] 깨진 문자 없이 읽을 수 있는 내용이 나온다

---

### ✅ T-03. hwpx 처리 방법 결정 및 준비 (30분)

**결정 필요 (I-1):** 아래 중 하나를 선택한다.

| 선택 | 방법 | 작업 |
|------|------|------|
| A | hwpx → PDF로 저장 후 database/ 교체 | 한글에서 "다른 이름으로 저장 → PDF" |
| B | hwpx 직접 파싱 (zip+XML 구조 활용) | `adm-zip` 라이브러리 설치 필요 |

**완료 기준:**
- [ ] A 선택: database/ 폴더에 .hwpx 대신 .pdf 파일이 들어있다
- [ ] B 선택: 터미널에서 hwpx 1개 텍스트가 정상 출력된다

> ⚠️ hwpx 파일이 13개 있으므로 A 선택 시 변환 작업 시간 별도 필요

---

## Phase 3. database/ 폴더 읽기 연결 (T-02, T-03 이후)

### ✅ T-04. 파일 읽기 모듈 작성 (1시간)
**작업:** `utils/fileReader.js` 를 새로 만들어 database/ 폴더 전체를 읽는다.

출력 형식 (guide.json과 동일하게):
```json
[
  {
    "id": "그룹웨어 비밀번호 재설정",
    "category": "그룹웨어 비밀번호 재설정",
    "keywords": ["비밀번호", "재설정", ...],
    "content": "추출된 텍스트...",
    "source": "그룹웨어 비밀번호 재설성.pdf"
  }
]
```

**완료 기준:**
- [ ] `node utils/fileReader.js` 실행 시 database/ 파일 수만큼 항목이 출력된다
- [ ] 각 항목에 source(파일명)가 올바르게 들어있다
- [ ] PDF와 hwpx 모두 content에 텍스트가 들어있다

---

### ✅ T-05. chat.js에서 guide.json 대신 database/ 읽도록 교체 (30분)
**작업:** `api/chat.js`의 `loadGuide()` 를 `fileReader.js` 호출로 교체한다.

**완료 기준:**
- [ ] 서버 재시작 후 "그룹웨어 비밀번호 재설정 방법" 질문 시 실제 PDF 내용이 답변으로 나온다
- [ ] 답변 하단 출처에 실제 파일명이 표시된다
- [ ] guide.json은 더 이상 답변에 영향을 주지 않는다

---

## Phase 4. 최종 검증 (T-05 이후)

### ✅ T-06. PRD 테스트 케이스 3개 직접 확인 (30분)
**작업:** 브라우저에서 직접 입력해서 기대 결과와 비교한다.

| # | 입력 | 기대 결과 | 결과 |
|---|------|---------|------|
| T-1 | 그룹웨어 비밀번호 재설정 방법 | 실제 PDF 내용 + 파일명 출처 | ⬜ |
| T-2 | 복무규정에서 연차는 며칠인가요 | 복무규정 내용 + 파일명 출처 | ⬜ |
| T-3 | 오늘 점심 뭐 먹을까요 | 노란 fallback 박스 + 총무팀 이메일 안내 | ⬜ |

**완료 기준:**
- [ ] 3개 테스트 케이스 모두 기대 결과와 일치한다
- [ ] T-3 fallback에 실제 총무팀 이메일과 내선번호가 맞게 표시된다

---

## Phase 5. Claude API 연결 (T-06 이후, 선택)

### ⬜ T-07. .env 파일에 API 키 입력 후 자연어 답변 확인 (30분)
**작업:** API 키를 발급받아 .env에 입력하고 목업 모드와 답변 품질을 비교한다.

```
# .env
ANTHROPIC_API_KEY=sk-ant-...
```

**완료 기준:**
- [ ] 같은 질문에 목업 모드(원문)와 API 모드(요약)의 차이가 확인된다
- [ ] API 모드에서 답변이 자연스러운 한국어 문장으로 출력된다
- [ ] 오류 없이 3초 이내 응답이 온다

---

## 작업 순서 요약

```
T-01 (버튼 교체)
  ↓
T-02 (PDF 설치 확인)  →  T-03 (hwpx 방법 결정)
          ↓                        ↓
         T-04 (파일 읽기 모듈)
                  ↓
              T-05 (서버 연결)
                  ↓
              T-06 (최종 테스트)
                  ↓
           T-07 (API 연결, 선택)
```

---

## Nope (이번 버전에서 하지 않음)

| 기능 | 이유 |
|------|------|
| 관리자 파일 업로드 UI | database/ 폴더 직접 추가로 대체 |
| 로그인 / 인증 | 사내 인트라넷 접근 제한으로 대체 |
| 대화 이력 저장 (DB) | 세션 내 대화로 충분 |
| 모바일 반응형 UI | PC 우선 검증 후 2단계 |
| 자동 승인·자동 판단 출력 | 사람 최종 확인 원칙 유지 |
| 실시간 문서 동기화 | 서버 재시작으로 갱신 허용 |
| 결제 기능 | 해당 없음 |

---

## 현재 막힌 점

| # | 이슈 | 해결 방법 |
|---|------|---------|
| I-1 | hwpx 처리 방법 미결정 | T-03에서 결정 |
| I-2 | 배포 위치 미확인 (인트라넷 vs 홈페이지) | IT팀 확인 필요 (T-06 이후) |
| I-3 | Claude API 키 미발급 | T-07에서 진행 (선택) |
